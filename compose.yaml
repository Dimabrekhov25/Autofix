services:
  postgres:
    container_name: postgres_autofix
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: autofix_db
      POSTGRES_USER: autofix
      POSTGRES_PASSWORD: autofix
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data: